package com.springboot.lesson3.repository;

import com.springboot.lesson3.model.LibRole;
import org.springframework.data.repository.CrudRepository;

public interface LibRoleRepository extends CrudRepository<LibRole, Long> {
}
